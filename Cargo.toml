[package]
name = "anonssh"
version = "0.2.0-beta.2"
edition = "2024"
license = "MIT"
description = "Single-process SSH client over embedded Arti Tor"

[dependencies]
anyhow = "1.0"
arti-client = { version = "0.46.0", default-features = false, features = ["tokio", "rustls", "static-sqlite", "pt-client", "compression", "geoip"] }
clap = { version = "4.5", features = ["derive"] }
crossterm = "0.29"
rand = "0.10"
rpassword = "7.4"
russh = { version = "0.63.3", default-features = false, features = ["flate2", "ring", "rsa"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std"] }
tempfile = "3.23"
tokio = { version = "1.47", features = ["macros", "rt-multi-thread", "io-std", "io-util", "signal"] }
tor-chanmgr = "0.46.0"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
strip = "symbols"

[dev-dependencies]
md5 = "0.8"
